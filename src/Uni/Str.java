package Uni;

public class Str {
    //public static void countChars(String s, int a[]) {
    //s = s.toLowerCase();
    //for (int i = 0; i < s.length(); i++) {
    //if (s.charAt(i) >= 97 && s.charAt(i) <= 122) //Oblast ot ASCII za malki bukvi
    // a[(s.charAt(i) - 97)]++;
    //else
    //if (s.charAt(i) >= 65 && s.charAt(i) <= 90) //Oblast ot ASCII za glavni bukvi
    //   a[(s.charAt(i) - 65)]++;
    //  }
    // System.out.println( Arrays.toString(a));
    //}

    //public static void main(String[] args) {
    // Scanner scan = new Scanner (System.in);
    // int array[] = new int[26];
    // System.out.println("Enter string:");
    // String str = scan.nextLine();
    //countChars(str, array);
    //for (int i = 0; i < array.length; i++) {
    //if (array[i] != 0) {
    //char ch = (char)(i + 97);
    //System.out.println("'" + ch + "'->" + array[i]);
    //}
    // }
}
